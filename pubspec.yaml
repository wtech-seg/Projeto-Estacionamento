name: wtech_estacionamento
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^2.0.7
  wtech_design_system:
    path: C:\Users\carol\StudioProjects\wtech_design_system

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.13.1
  flutter_native_splash: ^2.4.0

flutter:
  uses-material-design: true

flutter_native_splash:
  color: "#001727" # Cor de fundo (primary)
  image: assets/images/logobranca.png # sua logo (precisa ser PNG!)
  android: true
  ios: true
  web: false

flutter_launcher_icons:
  android: true
  ios: true
  image_path: assets/images/logobranca.png
  adaptive_icon_background: "#001727"
